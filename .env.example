# Shared secret for internal service-to-service endpoints
# (filldirtnearme rep-portal -> dumpsite-io Sarah APIs).
# Generate with:  openssl rand -hex 32
# Must be set identically in both Vercel projects. Rotate by updating both
# environments in the same deploy window.
INTERNAL_SERVICE_TOKEN=
